variables:

    backend_version:
        default: 5

    frontend_version:
        default: 10

    mail_sender:
        default: mock
        environments:
            test: sendgrid
            uat: sendgrid
            prod: sendgrid